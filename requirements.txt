# Runtime dependencies (all mainstream). For development install use:
#   pip install -e ".[dev]"
numpy>=1.24
pandas>=2.0
plotly>=5.24
dash>=2.17
# Hosted-deployment performance: Brotli/gzip response compression and fast
# JSON serialization (plotly auto-uses orjson when present).
flask-compress>=1.14
Brotli>=1.1
orjson>=3.9
# Production WSGI server (used by the Render deployment)
gunicorn>=21
